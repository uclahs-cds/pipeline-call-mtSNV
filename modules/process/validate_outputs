process validate_outputs {
   container  params.validate_docker_image

    input:
        path(file)
    
    script:
   """
   set -euo pipefail

   python3 -m validate -t sha512-gen ${file}
  
   """
}

