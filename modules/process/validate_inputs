//// Process ////

process Validate_Inputs {
   container "blcdsdockerregistry/validate:1.0.0"

    input:
    tuple( path(normal), path(tumour))   
    
    script:
   """
   set -euo pipefail

   python -m validate -t file-input ${normal}
   python -m validate -t file-input ${tumour}

   """
}
