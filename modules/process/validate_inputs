//// Process ////

process Validate_Inputs {
   container "blcdsdockerregistry/validate:1.0.0"

    input:
      tuple(path(x), val(type))    
    
    script:
   """
   set -euo pipefail

   python -m validate -t file-input ${x}

   """
}
