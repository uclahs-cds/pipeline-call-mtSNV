//// Process ////

process Validate_Inputs {
   container "${params.validate_docker_image}"

    input:
      tuple(
        val(type), 
        val(name),
        path(file)
        )    
    
    script:
   """
   set -euo pipefail
   
   python -m validate -t file-input ${file}
   """
}

